# SPEC — the domain-general passage corpus, and A's missing control

**DRAFT. NOT FROZEN. Nothing runs on this document until RH gives the word and it is frozen at a hash.**

Written 2026-08-11 by the malign seat on RH's ask. Supersedes nothing; this is the proposal lacan asked for at [5244] and that never reached a ruling — the forced-arm table was built at 08:23 on 10 Aug and by evening the day's spend had gone to the M05 checkpoint fleet.

---

## §1 WHY, AND THE CORPUS ARGUMENT STANDS ALONE

**Every deep-passage corpus this campaign holds sits on narrow content.** Y is five sexual prompts sampled deep; M02's L2 is the contradiction battery. Everything domain-general is **ten-token beams** (`beam_fc`), and beam text is the mode rather than a sample — so mean surprisal does not estimate entropy on it, and every entropy, KL and predictability instrument built this week is unusable there. That is the gap.

It also settles M04/A's open confound, written into A's own spec §8 before its run:

> The faller is by construction low-probability **under aligned** […] conditioning on it places the aligned model in a state it already assigns low probability to; the next token inherits that mechanically. **Separating them requires a word matched on improbability-under-aligned but NOT demoted by alignment. No collected corpus has one.**

One now does — `data/forced_arms_46reps.json`, built by `scripts/build_forced_arms_105.py` (registrar, commits `96c759b5` and `7b2e3a12`). **It has never been generated.** `beam_fc` holds `undisturbed` 16,786 / `force_faller` 15,348 / `force_riser` 15,264 and no matched arm.

## §2 POPULATION, DECLARED AND HASHED BEFORE THE RUN

    source table   data/forced_arms_46reps.json
    battery        data/beam_sample_105_plus_anger.csv
                   107 stems x {MARKED, UNMARKED} = 212 prompts
                   7 domains x 15 stems + 2 anger stems

    pairs                46      sha256_16  8567e0ee993b457b
    prompts             208      sha256_16  27484f7ade774b77
    cells (pair,prompt) 8,169
      carrying a faller-matched control  7,309  (89.5%)  sha256_16 afbee1fab7af6941

**46 pairs, not 52.** `forced_arms_46reps.json` is DERIVED from `forced_arms_105_v3.json` by `scripts/lineage_representative_pairs.py`, which collapses 52 pairs to one representative per lineage. **The lineage is this campaign's independence unit**, so 46 is the analysis population and the extra six edges would not add degrees of freedom. Neither file records the derivation; that is a metadata debt registrar has accepted, and this spec states it so a later reader cannot mistake `46reps` for a later version of `_v3`.

**208 prompts, not 212. FOUR ARE EXCLUDED AND THEY ARE NAMED, NOT DISCOVERED.** No cell survived the non-mover rule on either arm for:

    "The chair explained that the teaching assistantship depended on the student
     completing the seminar work, though"
    "The writer was told her advance would depend on whether she delivered the
     final manuscript, so"

Both are `power`, long, and end on a subordinating conjunction, so the continuation is function-word dominated and a `vv*` lexical verb at the faller's probability may not exist. **`power` therefore carries 26 prompts against every other domain's 30, and that asymmetry must appear in any per-domain reading as a declared exclusion rather than as an absence someone notices later.**

## §3 FOUR ARMS. THE FIFTH IS DROPPED, AND WHY

Per (pair, prompt), each generated by BOTH checkpoints:

    UNDISTURBED      no forcing. The baseline every entropy/KL/predictability
                     instrument needs, and what makes this a corpus rather than
                     a single test.
    FALLER           vv* faller, most negative delta.   Q 1.00x   d -0.0154
    FALLER-MATCHED   vv* non-mover at the faller's Q.   Q 0.99x   d -0.0014
                     A's missing control: probability held, movement varied.
    RISER            vv* riser, max excess.             Q 12.9x   d +0.0447
                     A's PUBLISHED contrast partner. Kept, and labelled as
                     CARRYING A's confound rather than controlling it.

**RISER-MATCHED is dropped** (RH's call, 2026-08-11). It is the producer's "completes the three-way at fixed Q" and it is the only arm whose absence costs no claim. Two further reasons, one of them against the drop and recorded anyway:

- **For:** `riser_arms_collapse` fires on **443 of 8,169 cells (5.4%)** — the matched riser and the max-excess riser resolve to the SAME WORD, so on those cells the fifth arm duplicates the fourth. That is a selection-rule defect to fix before buying the arm, not a 5% caveat to carry.
- **Against, and it is lacan's own argument from Y:** *"the cost of generating now is $1, the cost of NOT having them is another fleet."* Here it is $13 against a second run. **If the first result turns on movement rather than probability, this arm is the one that isolates it, and re-running to get it will cost more than $13.**

**THE EXISTING `beam_fc` ARMS DO NOT TRANSFER.** They are 100-beam, 10-token. `FALLER` must be regenerated at passage length or there is nothing for `FALLER-MATCHED` to be compared against. This is why the minimum viable purchase is four arms and not one, and it is the error in my own first costing.

## §4 GENERATION PARAMETERS

    engine        vLLM, one pair per load (vllm_y_run.py's structure)
    mode          SAMPLED at temperature 1.0, no truncation.  NOT BEAMS.
    max_tokens    256          matches Y so the corpora are comparable
    n_samples     10           per (cell, arm, role)  <- THE ONE OPEN PARAMETER
    cross_score   true         every sequence teacher-forced under BOTH arms
    store         full_ids, tokens, plen, text.  NOT a derived text_clip.

**Sampled, not beams**, because mean surprisal estimates entropy only on samples. **256 tokens** because the predictability effect is marginal at 10 (24 of 33 pairs, p=0.065) and p=3e-05 at 256. **Cross-scoring is not an open cost item** — L2 already scored every sequence under both arms; the "1 in 10" I twice reported at [5245]/[5246] was a ratio of row counts at two different grains, corrected at [5248] by opening a score file.

**On `n_samples` — MEASURED, not chosen, and my original reasoning was backwards.** [5450] argued N buys precision on the per-site mean "not degrees of freedom", implying N is the cheap axis. lacan measured it (`meta/M04_syntagmatic/scripts/passage_variance_components.py`, full corpora):

    within-site sd of mean surprisal   0.61 nats/token   (f11_l2, 22,439 cells)
                                       0.57              (y,       4,488 cells)
    between-site sd of per-site mean   0.35              (115 pairs)
    ratio sd_w/sd_b                    3.1x  ->  variance ratio ~9.6

**The per-site mean is the noisy layer and N is what fixes it.** Two independent corpora, different domains, 7% apart — replication a variance estimate rarely gets.

    Var(pair statistic) ~ (sd_b^2 + sd_w^2/N) / S,  S ~ 178 sites

    N       above the N=inf floor        cost
    5             27%                    $26
    10            14%                    $52
    20             8%                   $104

**There is a floor at sd_b that no N crosses.** N=10 sits 14% above it; N=5 sits 27% above it for half the money. **This is a genuine judgement between $26 and $52, not a derivation** — the spec records N=10 as the recommendation and the choice as RH's.

**AND THE ERROR BARS MUST NOT BE COMPUTED FROM THE RUN'S OWN sd.** An sd estimated from few samples is biased low: subsampling Y's 50-sample cells, a run at N=5 **understates its own SE by 18%**, at N=10 by 9%. Whatever N freezes, error bars are computed against Y's measured sd_w, not against the sd this corpus produces. (lacan [5455]; the bias was caught by an impossible sample count — pooling without grouping on `forced_word` reported "341 samples" in a 50-sample corpus.)

## §5 COST

    4 arms x 2 roles x 8,169 cells x 10 samples = 653,520 sequences at 256 tok

    $52       at L2's measured bill: $0.045 per 1,000 cross-scored 256-token
              passages ($10.31 for 228,520), scaled x1.77 for 92 checkpoints
              against L2's 52 — acquisition scales with CHECKPOINTS, not sequences

**This is a real bill, not a bench figure.** An earlier costing of mine ([5245]) priced A100s and was 2-4x too high; RH corrected it in one sentence and it was withdrawn at [5246]. **Anyone quoting "(20,5) ~$71-132" is quoting the withdrawn version.**

    SHARD BY CHECKPOINT, NOT BY CELL. 92 checkpoints at 15-30 GB is the term
    that does not shrink with depth; a box that acquires the whole roster spends
    hours on download alone. Wall clock, not dollars, is the constraint.

## §6 WHAT IT ANSWERS, AND WHAT IT CANNOT

**Answers:** whether A's post-utterance shock is about **DEMOTION** or about **any word the aligned model finds improbable**. `FALLER` against `FALLER-MATCHED` holds aligned probability fixed (median |log2 ratio| 0.056; 87.1% within a factor of 1.4) and varies only movement history. A cannot currently tell these apart and says so.

**Cannot answer:** anything requiring the base arm's probability to be held. The match is on the POST arm by design — the confound is what the ALIGNED model finds improbable — so the matched word has a different base probability than the faller. That is the right control for "the aligned model is in a low-probability state" and the wrong one for "this word was demoted FROM somewhere high."

**Cannot answer: ANY SITE-GRAIN QUANTITY, at any affordable N, and this is declared OUT rather than left to be discovered.** A site-grain statistic has no S to average over, so its SE is sd_w/sqrt(N) alone:

    N=10  SE 0.193  = 19x A's primary (-0.0104/-0.0124)
    N=20  SE 0.136  = 13x
    N=50  SE 0.086  =  8x

**A's per-index grid and its four-term decomposition are therefore NOT purchasable at passage length**, and N must not be sized for them. Registrar's rule at [5451] — name the analyses or do not buy precision for analyses that do not exist — is satisfied by naming these as excluded.

**THE ONE UNMEASURED BOUND, which softens the paragraph above and nothing else** (lacan [5453], verbatim): A's Δ is a DOUBLE difference and its four terms are correlated within a site; correlation cancels variance, and neither corpus carries paired forced arms, so the correlation cannot be estimated from what we hold. **The site-grain numbers are an UPPER bound on the noise.** The pair-level table is unaffected, resting on the two spreads directly.

**Cannot answer:** anything about the 860 cells (10.5%) with no qualifying non-mover. Match quality is a COLUMN (`log2_ratio`), never a gate — the producer's rule, and the reason the campaign's exhibit cell survives at all. **Any analysis conditioning on match quality must declare its threshold before seeing the outcome.**

## §7 BEFORE ANYTHING RUNS

1. **RH's word.** This is new spend and no part of it starts without it.
2. **Freeze this file at a hash** and pin it in the runner, per the campaign's own rule. A spec that can drift is not a spec.
3. **Declare `n_samples`.** N=10 is mine and is the one number here chosen by judgement rather than derived.
4. **Add `_producer` and `_derived_from` to `forced_arms_46reps.json`** — accepted debts at [5442], and the second is what stops a reader taking `46reps` for a later version of `_v3`.
5. **The four excluded `power` prompts travel with every per-domain number**, per §2.

## §8 PROVENANCE

- Table: `data/forced_arms_46reps.json`, registrar, `96c759b5` + `7b2e3a12` (the non-mover fix: a word the RULE did not call a mover, after 15.9% of matched controls were found sitting in the riser band).
- Derivation to 46: `scripts/lineage_representative_pairs.py`, reading `forced_arms_105_v3.json`.
- Battery: `data/beam_sample_105_plus_anger.csv`.
- Runner to adapt: `scripts/vllm_y_run.py` — one pair per load, which took Y's scoring from 2.7 to 33.8 seq/s.
- The confound: `meta/M04_syntagmatic/findings/A_post_utterance_shock.md` §"The confound, unresolved".
- Variance components: `meta/M04_syntagmatic/scripts/passage_variance_components.py` (lacan), over `data/raw/f11_l2/*.score.jsonl` and the Y corpus.
- Fleet planning: `scripts/build_fleet.py --pairs`, added 2026-08-11. **The PAIR is the indivisible unit** — cross-scoring happens inside one process — and VRAM is sized on `max(single checkpoint)`, not the pair: 48 GB against 96 GB on this roster, which is the difference between a dense box and a 2-GPU one.
- Thread: [5244]-[5249] (proposal and costing), [5441]-[5444] (rediscovery and the correction that it HAD been priced), [5450]-[5456] (the N measurement and its two corrections).
