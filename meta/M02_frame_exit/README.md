# M02 — Frame-exit: what contradiction does to the continuation

**The claim under test moved on 2026-08-08, and the movement is the module's
current content.** M02 began as "alignment resolves contradiction by leaving
the frame" (F11's discourse-grain extension). Three first-look instruments now
point the OTHER way — the contradiction cell exits *at or below* its poles and
its length-matched conjunction controls — and within the contradiction
passages, holding both poles is the modal outcome. Whether that inversion
survives power is what the frozen redo exists to decide. All of it enters the
redo as DECLARED PRIORS, never support.

## The map

    registrations/    the frozen redo registration (registration_m02_redo.md —
                      body hash 33536d93ab9abb9a; its EXECUTION BLOCK is the
                      live state of the cascade), the N3 execution addenda
                      (v3 current), the battery and generative-contradiction
                      plans, the channel-3 spec
    findings/         the write-ups (index below)
    scripts/          the marker battery, the sweeps, the quintuplet builder
                      (data/f11_quintuplets.json is BUILT — edit a source and
                      rebuild, never the output)
    results/          per-cell CSVs, roster/filters stamped per row

## findings/ index

| File | What it holds |
|---|---|
| [M02_eassist_ambient.md](findings/M02_eassist_ambient.md) | **The delivered finding**: aligned checkpoints emit assistant control tokens into raw continuation unbidden — sign roster-wide, magnitude owned by Falcon3 by name |
| [exit_markers_first_look.md](findings/exit_markers_first_look.md) | The four-design first look: the cloze negative closed, Q/A conversion thrice-surviving, the forced-arm scene-not-signifier split, the instrument facts |

## Key facts a consumer needs before touching this module's data

- The prompt populations live in `data/prompt_categorisation.json` (status
  filters are GROUP-WISE — a row-wise ACTIVE filter produces partial triplets
  that look like triplets).
- The assembled quintuplets (en+zh, controls inline) are
  `data/f11_quintuplets.json`; canonical-text ownership for shared prompts is
  `data/f11_canonical_texts.json`.
- `beam_fc` keys carry no `design` field — design lives in record VALUES.
- 12 of 68 beam_fc checkpoints beam-sampled under their own configs
  (within-checkpoint contrasts difference it out; cross-arm ones do not).
- f11_holy / f11_holy_zh: poles unmatched, shared BOTH cell with their _b
  twins — never pooled. f11_reason / _zh: the declared weak-manipulation
  negative control, outside the primary, run and reported beside it.

The registration's execution block and the claims register (article hub,
`notes/claims-register.md`) govern anything this page appears to assert.
