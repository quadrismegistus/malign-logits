# TODO: repo findings never retried in the meta/ campaigns

Compiled 2026-08-13 (lacan seat, RH's ask). Method: every `findings/F*.md`
checked against the six meta/ modules by construct, not by citation count --
a citation is not a retry (F31 is design input to M04, not retried by it) and
a retry may not cite the number (F35's retry is M01's Finding J, which never
says "F35"; F34's is O_crosslingual). Grades/statuses from frontmatter, with
GRADES.md's own warning that the fields lag git lineage. "Retried" means a
meta module re-tested the construct on M-era instruments and population.

## Never retried

| finding | grade/status | construct | note toward a retry |
|---|---|---|---|
| F03 | C/unaudited | generation defense-mechanism taxonomy (genre collapse, narrative sublimation, rotating defenses) | M06's passage corpus is the natural substrate; needs a coding instrument, not just metrics |
| F09 | C/unaudited | same base, different curriculum (Tulu 3.1 vs Llama 3.1 Instruct) | both lineages are in the roster; a dedicated two-curricula contrast has never been run on M-era instruments |
| F12 | C/retained-downgraded | alignment as fold; fold concentration predicts steerability | V killed six adjacent geometric instruments (the relation is local); any retry must answer V first |
| F15 | C/unaudited | surprisal x drift quadrants; base occupies breakdown (Q2), alignment drains it to metonymic/unmarked | IN PROGRESS: M06 plan being drafted on the passage corpus (this session) |
| F16 | C/unaudited | human-corpora anchors (dreams, waking, fiction, abstracts) in the same metric space | depends on F15's retry landing first; then rescore the human corpora with the M06 instrument |
| F17 | C/unaudited | MMD: alignment shifts WHAT is said (p=.0004), smooths HOW uniformly (p=.99) | M06-retriable; embedder choice must respect the mps/CPU policy |
| F18 | C/unaudited | entropy compression, base ~4 nats to aligned ~3.5 | sharpness alone identifies the arm at 0.87+ AUC (P armclf nuisance); a retry must say what compression adds beyond the confound it now names |
| F19 | C/rescoped | BOS/unconditional generation below Shannon 1.0 bits/char; feedback-loop reading | the BOS arm IS in the M-era battery (RUN_46494481); the sub-Shannon claim itself never recomputed; M03 flagged the territory and claimed nothing |
| F22 | C/unaudited | circuit decomposition: SFT carries 92% of residual broadening; class engine in preference stage | mechanism-level; no meta module touches internals except M01's logit-lens pilots, which stop at the unembedding |
| F23 | C/unaudited | reasoning distillation as a third alignment regime | reasoning rungs exist in twp for some lineages; no meta question has used them as a regime |
| F25 | C/rescoped | temporal alignment signature; foreclosure acquisition | X excludes F25's foreclosure tokens as junk-adjacent; the clinical-mechanism grid itself never rebuilt |
| F27 | C/unaudited | negative control: nudging does not reproduce displacement | cheap to rerun on the M-era battery; negative controls age when the battery changes |
| F28 | C/rescoped ("not established") | position-specific resistance trajectories; first-token spike | M04 charter reads its numbers as design input; the successor measurement M04 was chartered toward has not landed |
| F31 | C/rescoped | PERMANOVA: pretraining dominates alignment (97.8% family variance) | used everywhere as a design constraint; the decomposition itself never re-run on an M-era feature space |
| F32 | B/solid-by-design | template = task switch, not distribution filter | M02's E-ASSIST-AMBIENT is adjacent (frame emitted unbidden) but tests emission, not the switch construct |
| F33 | C/unaudited | scale effects: same mechanism, different displacement vocabulary | roster spans 360M-13B+ but scale has never been a declared variable in any meta contrast |

## Partially or implicitly retried (judgment calls, stated)

- **F02** (C/rescoped, cross-family alignment intensity): H2_alignment_depth
  is the construct rebuilt (per-family depth, 38 implementations, ICC ~0);
  the specific JS ranking of families never re-quoted.
- **F05** (D/rescoped): rerun already downgraded it (fixed-word-list
  artifact); M01's logit-lens pilots confirm the final-layer conclusion for
  displacement and find something else for contradiction. Retry of the
  original construct not owed; the pilots' scale-up is tracked in M01/TODO.
- **F26** (D/rescoped): variance headline dead; M05 A_acquisition refined
  the Think-SFT/DPO displacement sub-claim. The deleter/redirector typology
  itself never retried.
- **F20 family** (A/verified): verified by repo-era rebuilds (addendum,
  third-person, 24-base expansion), not by a meta module; M06's charter
  names it prior art on a different rung. A meta retry would be a new
  question, not owed maintenance.
- **F07** (C/unaudited): documentary (quotes the OLMo 3 report); a retry is
  a citation check, not an experiment.

## Retried, for the record (the complement)

F01/F06/F08/F10 (M01 core: movement rule, K site-specificity, T taxonomy, U
ablations), F04 (M05 + U timing), F11 (M02), F13/F14 (M04), F21 (M03), F24
(M05), F34 (M01 O + zh instruments), F35 (M01 J: SSMs straddle the
transformer), F36 (M01 X: euphemism vs proximity is X's question), F40
(movement words are core), F41 (M01 K is the successor), F39 (already
M-era).
