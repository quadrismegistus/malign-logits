{
 "_what": "Index into the .f16 logit payloads written by the 2026-08-01 run. Value is {file, row, dim}; the vectors are memmapped on read and never copied into the store.",
 "_integrity": "SELF-CONSISTENT, NOT SOURCE-VERIFIED. What was checked is STRUCTURE: byte size against the row pointers, and row count against the scored-cell count, on every file. What was NEVER checked is CONTENT against the cloud instances -- the handover verification compared *.jsonl only, which is 4% of the bytes, and both sources were destroyed before the omission was noticed. Ruled [3009] Rider 1; this sentence is inherited by every reader.",
 "_root": "paths are BASENAMES resolved against MALIGN_LOGIT_ROOT (default data/raw/cloud_run_20260801). Moving the payloads is a config change, not a re-index.",
 "_load_bearing": "THE .f16 FILES ARE INFRASTRUCTURE, NOT RUN OUTPUT. Deleting them silently empties this cache.",
 "entries": 5166,
 "files": 2,
 "files_provenance": {
  "inceptionai__jais-family-6p7b-chat.f16": {
   "dim": 84992,
   "dtype": "float16",
   "n_rows": 2583,
   "itemsize": 2,
   "bytes": 439068672,
   "check": "bytes == n_rows * dim * itemsize"
  },
  "inceptionai__jais-family-6p7b.f16": {
   "dim": 84992,
   "dtype": "float16",
   "n_rows": 2583,
   "itemsize": 2,
   "bytes": 439068672,
   "check": "bytes == n_rows * dim * itemsize"
  }
 }
}